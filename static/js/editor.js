// Generated by CoffeeScript 1.3.3
(function() {
  var myCodeMirror, obj, view_options;

  console.log('hellop');

  myCodeMirror = null;

  $(function() {
    return $.get('/static/js/ai.txt', function(data) {
      var d;
      d = $('#aicode')[0].contentDocument.body.innerText;
      return myCodeMirror = CodeMirror($('#codemirror')[0], {
        value: d,
        mode: "coffeescript",
        theme: "solarized-dark",
        lineNumbers: true,
        height: "100%",
        tabSize: 2,
        autofocus: true
      });
    });
  });

  $(function() {
    return $('.run.btn').click(function() {
      var cs, js;
      cs = myCodeMirror.getValue();
      js = CoffeeScript.compile(cs);
      return $('#mote iframe')[0].contentWindow["eval"](js);
    });
  });

  obj = {
    message: 'dat.gui',
    speed: 0.8,
    displayOutline: false,
    explode: console.log
  };

  view_options = {
    delay: 0.1
  };

  /*
  $ ->
    gui = new dat.GUI autoPlace: false
    $('.gui .mote').append gui.domElement
    _add = (a, b, c)-> gui.add view_options, a, b, c
    _add 'delay', 0.01, 1
  
    gui = new dat.GUI autoPlace: false
    $('.gui .code').append gui.domElement
    gui.add obj, 'message'
    gui.add obj, 'speed', -5, 5
    gui.add obj, 'displayOutline'
    gui.add obj, 'explode'
  */


}).call(this);
